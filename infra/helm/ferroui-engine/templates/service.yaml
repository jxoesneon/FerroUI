apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferroui-engine.fullname" . }}
  labels:
    {{- include "ferroui-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ferroui-engine.selectorLabels" . | nindent 4 }}
